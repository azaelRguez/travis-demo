language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  # run tests to check pep8 style guides
  - flake8 pytiny/ --exit-zero
  - flake8 tests/ --exit-zero
  # run unit tests
  - python tests/test.py
